{{#extends 'base'}}
{{#block 'content'}}

    <h3>Updating Service {{service.name}}</h3>
    <form method="post">
        {{{form}}}
        <input type="submit" value="Update" class="btn btn-primary mt-3"/>
    </form>

{{/block}}
{{/extends}}